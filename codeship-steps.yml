- type: parallel
  steps:
    - name: build
      service: app
      command: yarn build
    - name: test
      service: app
      command: yarn test-ci
- name: deploy
  service: awsdeployment
  command: ls
  # command: ls && aws s3 cp /deploy s3://codeship-test-bucket --recursive
